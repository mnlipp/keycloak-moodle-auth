A Keycloak extension that uses a Moodle installation for authentication.

Keycloak Moodle Authenticator
=============================

A Keycloak extension that uses a Moodle instance for authentication
(and registration).